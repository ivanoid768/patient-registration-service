directive @authRequired(role: Role) on FIELD_DEFINITION

type Query {
  	me: User! @authRequired(role: Doctor)
}

type Mutation {
	_empty: String
}

# TODO: move to graphql common file

enum Role {
	Owner
    Receptionist
    Doctor
}

type User {
  	id: ID!
	name: String!
	surname: String!
	middlename: String
	email: String
	phone: String
	role: Role!
}
