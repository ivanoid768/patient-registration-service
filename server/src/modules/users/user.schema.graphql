directive @authRequired(role: Role) on FIELD_DEFINITION

type Query {
  	me: User! @authRequired(role: Doctor)
}

enum Role {
	Owner
    Receptionist
    Doctor
}

type User {
  	id: ID!
	name: String!
	surname: String!
	middlename: String
	email: String
	phone: String
	role: Role!
}
